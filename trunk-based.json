{
  "trunk": "main",
  "release_strategy": "trunk",
  "deployment_branch": null,
  "leaf_prefix": "leaf",
  "leaf_max_hours": 24,
  "max_active_leaves": 3,
  "verify_command": "uv run --no-sync pytest -q",
  "allow_direct_trunk_commits": false,
  "merge_style": "no-ff",
  "review": "self-on-green",
  "feature_flags": {
    "mechanism": null,
    "how": "No flag library in this repo. The nearest mechanism is a key in src/config.py _DEFAULTS plus the matching Config field, overridable from config.yaml - default the new path off and gate on cfg.<key>. Introduce a real flag module only when a change actually needs to land dark."
  }
}
